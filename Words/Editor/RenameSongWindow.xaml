<Window x:Class="Words.Editor.RenameSongWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lied umbenennen" Height="135" Width="300"
        MinHeight="135" MaxHeight="135"
        MinWidth="300" MaxWidth="500"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" 
        WindowStyle="SingleBorderWindow"
        FocusManager.FocusedElement="{Binding ElementName=newNameTextBox}">
    <StackPanel Orientation="Vertical">
        <Label HorizontalAlignment="Left" VerticalAlignment="Top">Neuer Name:</Label>
        <TextBox x:Name="newNameTextBox" Height="Auto" Margin="5" Style="{StaticResource textBoxInError}">
            <TextBox.Text>
                <Binding Path="SongName" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <DataErrorValidationRule/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" FlowDirection="RightToLeft">
            <Button Content="Abbrechen" IsCancel="True" Width="75" Margin="3"/>
            <Button Content="OK" IsDefault="True" Margin="3" Width="75" Click="Button_Click" />
        </StackPanel>
    </StackPanel>
</Window>
